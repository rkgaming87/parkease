import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Park Ease | Parking Ticket Automation",
  description:
    "Smart, automated parking ticket management system — vehicle entry, exit, fare calculation, and reporting in one platform.",
  keywords: ["parking", "ticket", "automation", "ParkEase", "vehicle management"],
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
